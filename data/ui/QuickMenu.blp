using Gtk 4.0;
using Astal 4.0;

template $QuickMenu: Astal.Window {
    styles [
        "quick_settings_window"
    ]

    title: "QuickMenu";
    namespace: "QuickMenu";
    anchor: right | bottom;
    margin-right: 5;
    margin-bottom: 5;

    Box {
        orientation: vertical;
        spacing: 10;

        $Settings {
            hexpand: true;
            vexpand: false;
        }

        $SliderBox {
            hexpand: true;
            vexpand: false;
        }

        $PowerBox {
            hexpand: true;
            vexpand: false;
        }
    }
}
