using Gtk 4.0;
using Adw 1;

template $QBluetooth: Box {
  orientation: vertical;

  Adw.HeaderBar {
    show-end-title-buttons: false;

    [end]
    Button {
      tooltip-text: "Discover";
      clicked => $refresh();

      Image scan_button_image {
        icon-name: "view-refresh-symbolic";
      }
    }
  }

  $ScrollableIndicatorMenu scrolled_window {
    vexpand: true;

    styles [
      "background",
      "padding_10",
    ]

    ListBox blue_list {
      selection-mode: none;
    }
  }
}
